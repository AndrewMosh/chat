# Используем официальный образ Node.js
FROM node:latest

# Установка рабочей директории внутри контейнера
WORKDIR /app

# Копируем зависимости и устанавливаем их
COPY package*.json ./
RUN npm install

# Копируем все файлы клиента в контейнер
COPY . .



# Определите порт, который будет слушать ваше React приложение
EXPOSE 3000

# Запускаем React приложение при старте контейнера
CMD ["npm", "start"]
